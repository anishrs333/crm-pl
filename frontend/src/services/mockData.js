/**
 * Mock data has been completely deprecated and removed.
 * The application is now fully connected to the live Django REST Framework backend.
 */

export const initialUsers = [];
export const initialCustomers = [];
export const initialLeads = [];
export const initialOpportunities = [];
export const initialQuotations = [];
export const initialProducts = [];
export const initialTasks = [];
export const initialFollowUps = [];
export const initialActivities = [];
export const initialNotifications = [];
